[project]
name = "reservation-api"
version = "0.1.0"
description = "Reservation API for testing strategy experiments"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.115.12",
    "uvicorn==0.34.2",
    "SQLAlchemy==2.0.40",
    "PyMySQL==1.1.1",
    "alembic==1.15.2",
    "pydantic-settings==2.9.1",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
